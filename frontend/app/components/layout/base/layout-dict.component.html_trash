<ng-container>
  <modules-layout
    [data]="data[layout.key]"
    [layout]="computedLayout.items"
    [parentContext]="context"
    (onAction)="processAction($event)"
  >
  </modules-layout>
</ng-container>
