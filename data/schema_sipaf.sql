-- schema sipaf

CREATE SCHEMA IF NOT EXISTS sipaf;

CREATE TABLE IF NOT EXISTS sipaf.t_passages_faune (
    id_pf SERIAL NOT NULL,
    uuid_pf UUID NOT NULL DEFAULT public.uuid_generate_v4(),
    geom GEOMETRY(GEOMETRY,4326),
	pi_ou_ps VARCHAR,
	geom_wkt VARCHAR,
	PK FLOAT,
	PR FLOAT,
	PR_abs FLOAT,
	Y FLOAT,
	X FLOAT,
	id_op VARCHAR,
	nom_pf VARCHAR,
	Cd_Com VARCHAR,
	anRefCom VARCHAR,
	issu_requa VARCHAR,
	date_creat VARCHAR,
	date_requal VARCHAR,
	larg_ouvrag FLOAT,
	haut_ouvrag FLOAT,
	long_franch FLOAT,
	diam FLOAT,
	haut_disp FLOAT,
	larg_disp FLOAT,
	specificit VARCHAR,
	lb_typ_ouv VARCHAR,
	lb_materiaux VARCHAR,
	ep_materiaux VARCHAR,
	OH VARCHAR,
	OH_position VARCHAR,
	OH_caract VARCHAR,
	OH_banqu VARCHAR,
	OH_tirant VARCHAR,
	id_cer VARCHAR,
	id_corr VARCHAR,
	nom_corr VARCHAR,
	id_resv VARCHAR,
	nom_resv VARCHAR,
	id_obst VARCHAR,
	nom_obst VARCHAR,
	comment VARCHAR
);
