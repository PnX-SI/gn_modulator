{
    "$id:": "references/schema/import",
    "properties": {
        "import_name": {
            "type": "string"
        },
        "items": {
            "type": "array",
            "items": {
                "required": ["schema_name", "data"],
                "type": "object",
                "properties": {
                    "schema_name": {
                        "type": "string"
                    },
                    "data": {
                      "type": "string",
                      "title": "Lien vers le fichier (csv) qui contient les données"
                    },
                    "pre_process": {
                      "type": "string",
                      "title": "Lien vers le fichier (sql) qui contient les vue de mapping"
                    },
                    "keep_raw": {
                      "type": "boolean",
                      "title": "Si l'on souhaite garder la table d'import du fichier csv pour la réutiliser ensuite"
                    }
                }
            }
        }
    },
    "required": ["import_name", "items"]
}